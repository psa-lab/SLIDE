#include <math.h>

double distance ( double  pos1[3],
		  double  pos2[3] )
{
   double dx, dy, dz;
 
   dx = pos1[0] - pos2[0];
   dy = pos1[1] - pos2[1];
   dz = pos1[2] - pos2[2];
 
   return sqrt ( dx * dx + dy * dy + dz * dz );
}
